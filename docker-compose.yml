services:
  app:
    build: .
    container_name: gerador-landingpage
    ports:
      - "8765:8080"
    volumes:
      - .:/app
      - ./jobs:/app/jobs
    working_dir: /app
    restart: unless-stopped
